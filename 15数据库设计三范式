【重点】

什么是设计范式
    按照三范式设计的表不会出现数据冗余

三范式的内容
    1.任何一个表都应该有主键，且任何一个字段原子性不可再分
    2.所有非主键字段完全依赖主键，不能产生部分依赖（复合主键可能产生部分依赖）
        多对多的关系通过三张表解决，多一张关系表，关联两个外键
        多对多三张表，关系表俩外键
    3.所有非主键字段直接依赖主键，不能产生传递依赖
        一对多两张表，多的表加外键

【有时会用冗余换执行速度，多张表连接查询速度慢】